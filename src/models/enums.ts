export enum UserStatusEnum {
  Pending = 'pending',
  Rejected = 'rejected',
  Approved = 'approved',
}

export enum LanguageEnum {
  English = 'en',
  Spanish = 'es',
  French = 'fr',
  German = 'de',
  Italian = 'it',
}

export enum PostingOccurenceEnum {
  Daily = 'daily',
  Weekly = 'weekly',
  Monthly = 'monthly',
  Yearly = 'yearly',
  Never = 'never',
}

export enum ProductStatusEnum {
  Pending = 'pending',
  Rejected = 'rejected',
  Approved = 'approved',
}
